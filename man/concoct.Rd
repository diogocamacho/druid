% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concoct.R
\name{concoct}
\alias{concoct}
\title{concoct: DRUID wrapper}
\usage{
concoct(
  dge_matrix,
  num_random = 1000,
  druid_direction = c("neg", "pos"),
  fold_thr = 0,
  pvalue_thr = 0.05,
  entrez,
  dataset = NULL,
  tfidf_mode = c("geom_tf", "combined", "binary", "drug_tfidf"),
  min_matches = 3,
  n_cores = 1L
)
}
\arguments{
\item{dge_matrix}{Nx2 matrix: column 1 log2 fold-change, column 2 p-value.}

\item{num_random}{Number of random sets for the empirical null. Defaults to 1000.}

\item{druid_direction}{\code{"pos"} or \code{"neg"}. Defaults to \code{"neg"}.}

\item{fold_thr}{Absolute log2FC gate. Defaults to 0.}

\item{pvalue_thr}{P-value gate. Defaults to 0.05.}

\item{entrez}{Entrez IDs aligned to \code{dge_matrix} rows.}

\item{dataset}{Dataset name or id (\code{cmap}, \code{lincs}, ..., \code{all}).
If missing, falls back to interactive \code{\link{compendium_selection}}.}

\item{tfidf_mode}{Corpus weighting mode. Defaults to \code{"geom_tf"}
(geometric mean of dual TF-IDF views). Also \code{"combined"},
\code{"binary"}, \code{"drug_tfidf"}.}

\item{min_matches}{Minimum feature overlaps to report. Defaults to 3.}

\item{n_cores}{Cores for the empirical null (fork). Defaults to 1.}
}
\value{
A tibble sorted by decreasing DRUID score.
}
\description{
Start-to-finish wrapper to identify drug profiles that mimic or revert a
query phenotype. Computes cosine similarity to the drug corpus, an empirical
null, and DRUID scores.
}
